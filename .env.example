# Server-only — read by api/orders.js (Vercel serverless function).
# Do NOT prefix these with VITE_: that would inline them into the public
# client bundle, which is exactly the exposure this file replaces.
ERP_API_URL=
API_KEY=
